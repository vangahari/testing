name: testing the env variables

on:
#   push:
#     branches:
#       - '**'
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - closed
jobs:
  testing_variables:
    runs-on: ubuntu-latest
    steps:
       
     - name: print GITHUB_HEAD_REF
       run: echo $GITHUB_HEAD_REF >> hari.txt
       
     - name: cat file
       run: cat hari.txt
